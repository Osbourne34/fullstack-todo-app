import React from 'react';

import TextField from '@mui/material/TextField';

export const SearchCategory = () => {
    return (
        <TextField variant="standard" label="Поиск по категорий" fullWidth />
    );
};
